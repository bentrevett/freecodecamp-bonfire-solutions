function boo(bool) {
  // What is the new fad diet for ghost developers? The Boolean.
  return bool === true || bool === false ? true : false;
}

//return true if the argument is a boolean, return false if not
//boo(null);